<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VascscApp.Views.ItemDetailPage"
             Title="{Binding Title}">
    <StackLayout Spacing="20" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="120" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
         
            </Grid.ColumnDefinitions>
            <Label Text="{Binding Item.Text}" Grid.Row="0" Grid.Column="0"  FontAttributes="Bold"/>
            <Image Source="{Binding Item.Image}" Grid.Row="1" Grid.Column="0" />

            <Label Text="{Binding Item.Description}" Grid.Row="2" Grid.Column="0"  />
          
        </Grid>
        
        
      
    </StackLayout>
</ContentPage>